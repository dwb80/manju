# 剧本中心的职责边界与工业化生产阶段划分

> **文档版本**: V1.0  
> **创建时间**: 2026-07-10  
> **文档状态**: 待审核

---

## 一、核心观点（用户完全正确）

**剧本中心阶段只需要角色和场景的文字描述，不需要具体图片。**

图片生产在后续阶段进行，这符合工业化生产的阶段划分原则。

---

## 二、工业化生产阶段划分

### 2.1 完整的工业化生产流程

```
阶段0：项目立项
└── 确定项目基本信息（标题、类型、风格等）

阶段1：剧本中心（纯文字创作）⭐ 当前讨论
├── 剧本创作（文字内容）
├── 角色设定（文字描述）
├── 场景设定（文字描述）
├── 对白编写
├── 剧情结构
└── 人物关系

阶段2：资产生产（图片生成）
├── 角色工厂 → 根据角色文字描述生成角色图片
├── 场景工厂 → 根据场景文字描述生成场景图片
├── 道具工厂 → 根据道具文字描述生成道具图片
└── 风格设定 → 确定整体视觉风格

阶段3：分镜中心（镜头设计）
├── 分镜拆分 → 将剧本拆分为镜头
├── 镜头设计 → 确定景别、运动、时长
├── 角色站位 → 确定角色在场景中的位置
└── 分镜图片 → 生成分镜参考图

阶段4：视频中心（视频生成）
├── 图片优化 → 优化分镜图片质量
├── 视频生成 → 生成分镜视频
├── 视频合成 → 合成完整视频
└── 后期处理 → 音效、配乐、字幕

阶段5：发布中心（成品输出）
├── 视频导出 → 导出最终视频
├── 格式转换 → 转换为不同格式
└── 发布分发 → 发布到不同平台
```

---

### 2.2 阶段依赖关系

```
剧本中心（文字）
    ↓ 提供角色/场景文字描述
资产生产（图片）
    ↓ 提供角色/场景/道具图片
分镜中心（镜头）
    ↓ 提供分镜图片和视频参考
视频中心（视频）
    ↓ 提供完整视频
发布中心（成品）
```

**关键原则**: 每个阶段只处理该阶段应该处理的内容，不要跨阶段工作。

---

## 三、剧本中心的职责边界

### 3.1 剧本中心负责的内容（纯文字）

| 内容类型 | 详细说明 | 示例 |
|---------|---------|------|
| **剧本内容** | 完整的剧本故事文本 | Scene01: 茶信馆门口，林逸推开木门... |
| **角色设定** | 角色的文字描述（性格、外观、年龄） | 林逸：年轻男性，长发，白衣，性格冷淡... |
| **场景设定** | 场景的文字描述（地点、氛围、时间） | 茶信馆：古朴茶馆，木质结构，暖色调... |
| **对白编写** | 角色的对话内容 | 林逸：与你无关。萧晓：终于舍得出来了？ |
| **剧情结构** | 剧情的起承转合 | 第一集：相遇 → 冲突 → 分离 |
| **人物关系** | 角色之间的关系 | 林逸（男主角）- 萧晓（女主角）：恋人关系 |

---

### 3.2 剧本中心不负责的内容

| 内容类型 | 负责阶段 | 原因 |
|---------|---------|------|
| **角色图片** | 资产生产（角色工厂） | 图片生成是独立的资产生产环节 |
| **场景图片** | 资产生产（场景工厂） | 图片生成是独立的资产生产环节 |
| **道具图片** | 资产生产（道具工厂） | 图片生成是独立的资产生产环节 |
| **分镜图片** | 分镜中心 | 分镜需要基于资产图片设计 |
| **视频生成** | 视频中心 | 视频需要基于分镜图片生成 |

---

## 四、角色设定的文字描述规范

### 4.1 角色文字描述包含的信息

```typescript
interface CharacterDescription {
  // 基本信息
  name: string;              // 角色名称：林逸
  age: string;               // 年龄：20岁
  gender: string;            // 性别：男
  
  // 外观描述
  appearance: {
    height: string;          // 身高：180cm
    bodyType: string;        // 体态：瘦削
    hair: string;            // 发型：长发，黑色
    eyes: string;            // 眼睛：深邃，黑色
    skin: string;            // 皮肤：白皙
    clothing: string;        // 服装：白色古装长袍
    features: string[];      // 特征：剑眉、薄唇、冷峻
  };
  
  // 性格描述
  personality: {
    traits: string[];        // 性格特征：冷淡、内敛、聪明
    values: string[];        // 价值观：重情重义、不轻易承诺
    flaws: string[];         // 缺陷：不善表达、过于理性
  };
  
  // 背景故事
  background: string;        // 背景故事：林逸，孤儿，从小在茶信馆长大...
  
  // 角色关系
  relationships: Array<{
    targetCharacter: string; // 目标角色：萧晓
    relationshipType: string; // 关系类型：恋人
    description: string;     // 关系描述：青梅竹马，两情相悦
  }>;
  
  // 角色弧线
  arc: {
    startingPoint: string;   // 起点：冷漠、封闭
    turningPoint: string;    // 转折点：与萧晓分离后觉醒
    endingPoint: string;     // 终点：勇敢表达情感
  };
}
```

---

### 4.2 角色文字描述示例

```
角色名称：林逸

【基本信息】
- 年龄：20岁
- 性别：男
- 身高：180cm

【外观描述】
- 体态：瘦削，挺拔
- 发型：长发，黑色，散落在肩
- 眼睛：深邃，黑色，目光冷峻
- 皮肤：白皙
- 服装：白色古装长袍，袖口绣有云纹
- 特征：剑眉、薄唇、轮廓分明、气质冷淡

【性格特征】
- 性格：冷淡、内敛、聪明、重情重义
- 价值观：不轻易承诺，承诺必达
- 缺陷：不善表达情感，过于理性

【背景故事】
林逸，孤儿，从小在茶信馆长大，被茶信馆老板收养。
性格冷淡，不善言辞，但内心重情重义。
与萧晓是青梅竹马，两情相悦，但因性格冷淡而迟迟未表白。

【角色关系】
- 萧晓（女主角）：青梅竹马，恋人关系
- 茶信馆老板：养父，尊敬关系

【角色弧线】
起点：冷漠、封闭，不敢表达情感
转折点：与萧晓分离后，意识到自己的心意
终点：勇敢表达情感，守护萧晓
```

---

## 五、场景设定的文字描述规范

### 5.1 场景文字描述包含的信息

```typescript
interface SceneDescription {
  // 基本信息
  name: string;              // 场景名称：茶信馆
  type: string;              // 场景类型：室内/室外
  location: string;          // 地点：长安城东街
  
  // 空间描述
  space: {
    size: string;            // 空间大小：中型茶馆，约100平米
    layout: string;          // 空间布局：正厅、侧厢、后院
    architecture: string;    // 建筑风格：古朴，木质结构
    furniture: string[];     // 家具：木桌、木椅、茶具
  };
  
  // 氛围描述
  atmosphere: {
    time: string;            // 时间：白天/夜晚/黄昏
    weather: string;         // 天气：晴天/雨天/雪天
    lighting: string;        // 光线：暖光/冷光/自然光
    color: string;           // 色调：暖色调/冷色调
    mood: string;            // 情绪：温馨/紧张/神秘
  };
  
  // 声音描述
  sound: {
    ambient: string[];       // 环境音：茶水沸腾声、木门吱呀声
    music: string;           // 背景音乐：古琴曲
  };
  
  // 历史背景
  history: string;           // 历史：茶信馆已有50年历史...
  
  // 场景功能
  function: string[];        // 功能：喝茶、休息、情报交换
}
```

---

### 5.2 场景文字描述示例

```
场景名称：茶信馆

【基本信息】
- 场景类型：室内
- 地点：长安城东街

【空间描述】
- 空间大小：中型茶馆，约100平米
- 空间布局：正厅（喝茶区）、侧厢（包间）、后院（厨房）
- 建筑风格：古朴，木质结构，青瓦屋顶
- 家具：木桌（8张）、木椅（32把）、茶具（瓷制）、屏风（2扇）

【氛围描述】
- 时间：白天，上午
- 天气：晴天
- 光线：暖光，透过木质窗棂射入，形成斑驳光影
- 色调：暖色调，以棕色、米色为主
- 情绪：温馨、安静、略带神秘

【声音描述】
- 环境音：茶水沸腾声、木门吱呀声、茶客低语声、远处街道喧嚣
- 背景音乐：古琴曲《高山流水》

【历史背景】
茶信馆已有50年历史，是长安城最古老的茶馆之一。
传说这里不仅是喝茶的地方，也是江湖人士交换情报的秘密场所。

【场景功能】
- 喝茶：普通茶客喝茶休息
- 休息：旅人歇脚
- 情报交换：江湖人士秘密交流
```

---

## 六、工作流程示例

### 6.1 完整的角色生产流程

```
阶段1：剧本中心（文字描述）
├── 剧本创作：确定角色"林逸"
├── 角色设定：编写角色文字描述
│   ├── 基本信息：20岁，男，180cm
│   ├── 外观描述：长发，白衣，剑眉，薄唇
│   ├── 性格特征：冷淡，内敛，重情重义
│   └── 背景故事：孤儿，茶信馆长大
└── 输出：角色文字描述文档

    ↓ 提供文字描述

阶段2：角色工厂（图片生成）
├── 读取角色文字描述
├── AI生成角色图片（3-5张）
│   ├── 角色图1：正面
│   ├── 角色图2：侧面
│   └── 角色图3：背面
├── 用户筛选：选择可用的图片
└── 输出：角色资产（包含图片）

    ↓ 提供角色资产

阶段3：分镜中心（镜头设计）
├── 读取剧本和角色资产
├── 设计分镜：确定角色在场景中的位置
├── 生成分镜图片：基于角色资产
└── 输出：分镜图片

    ↓ 提供分镜图片

阶段4：视频中心（视频生成）
├── 读取分镜图片
├── 生成视频：角色在场景中的动态表现
└── 输出：视频片段
```

---

### 6.2 完整的场景生产流程

```
阶段1：剧本中心（文字描述）
├── 剧本创作：确定场景"茶信馆"
├── 场景设定：编写场景文字描述
│   ├── 基本信息：室内，长安城东街
│   ├── 空间描述：100平米，木质结构
│   ├── 氛围描述：暖光，温馨，安静
│   └── 历史背景：50年历史，情报交换地
└── 输出：场景文字描述文档

    ↓ 提供文字描述

阶段2：场景工厂（图片生成）
├── 读取场景文字描述
├── AI生成场景图片（3-5张）
│   ├── 场景图1：正面视角
│   ├── 场景图2：侧面视角
│   └── 场景图3：俯视视角
├── 用户筛选：选择可用的图片
└── 输出：场景资产（包含图片）

    ↓ 提供场景资产

阶段3：分镜中心（镜头设计）
├── 读取剧本和场景资产
├── 设计分镜：确定镜头在场景中的运动
├── 生成分镜图片：基于场景资产
└── 输出：分镜图片

    ↓ 提供分镜图片

阶段4：视频中心（视频生成）
├── 读取分镜图片
├── 生成视频：镜头在场景中的运动
└── 输出：视频片段
```

---

## 七、为什么剧本中心只需要文字描述？

### 7.1 专业分工原则

| 阶段 | 专业技能 | 工作内容 |
|------|---------|---------|
| **剧本中心** | 文字创作 | 剧本、角色、场景的文字描述 |
| **资产生产** | 图片生成 | 角色、场景、道具的图片生成 |
| **分镜中心** | 镜头设计 | 镜头、运动、时长的设计 |
| **视频中心** | 视频生成 | 视频、音效、配乐的生成 |

**优势**:
- ✅ 每个阶段专注自己的专业领域
- ✅ 避免跨阶段工作导致的效率低下
- ✅ 支持并行工作（不同阶段可同时进行）

---

### 7.2 迭代优化原则

```
第1轮迭代：
剧本中心 → 角色文字描述 → 角色工厂 → 角色图片
    ↓ 发现角色描述不够详细
剧本中心 → 修改角色描述 → 角色工厂 → 重新生成图片

第2轮迭代：
剧本中心 → 场景文字描述 → 场景工厂 → 场景图片
    ↓ 发现场景氛围不准确
剧本中心 → 修改场景描述 → 场景工厂 → 重新生成图片
```

**优势**:
- ✅ 文字修改成本低，图片生成成本高
- ✅ 先在文字层面确认，再进行图片生产
- ✅ 避免反复生成图片导致的资源浪费

---

### 7.3 成本控制原则

| 工作内容 | 成本 | 修改难度 |
|---------|------|---------|
| **文字描述** | 低 | 容易修改 |
| **图片生成** | 高 | 需要重新生成 |
| **视频生成** | 更高 | 需要重新生成 |

**原则**: 在低成本阶段完成确认，再进行高成本阶段的生产。

---

## 八、实施建议

### 8.1 剧本中心界面设计（纯文字）

```
┌─────────────────────────────────────────────────────────────┐
│  剧本中心 - 角色设定                                         │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  角色名称：[林逸                                       ]    │
│                                                             │
│  基本信息：                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 年龄：[20岁]  性别：[男]  身高：[180cm]             │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  外观描述：                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 体态：瘦削，挺拔                                     │   │
│  │ 发型：长发，黑色，散落在肩                           │   │
│  │ 眼睛：深邃，黑色，目光冷峻                           │   │
│  │ 皮肤：白皙                                           │   │
│  │ 服装：白色古装长袍，袖口绣有云纹                     │   │
│  │ 特征：剑眉、薄唇、轮廓分明、气质冷淡                 │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  性格特征：                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 性格：冷淡、内敛、聪明、重情重义                     │   │
│  │ 价值观：不轻易承诺，承诺必达                         │   │
│  │ 缺陷：不善表达情感，过于理性                         │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
│  背景故事：                                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │ 林逸，孤儿，从小在茶信馆长大...                      │   │
│  └─────────────────────────────────────────────────────┘   │
│                                                             │
├─────────────────────────────────────────────────────────────┤
│                          [保存]  [取消]                      │
└─────────────────────────────────────────────────────────────┘
```

---

### 8.2 工作流程提示

在剧本中心界面中，应该显示明确的工作流程提示：

```
┌─────────────────────────────────────────────────────────────┐
│  💡 工作流程提示                                             │
│                                                             │
│  当前阶段：剧本创作（文字）                                   │
│                                                             │
│  下一步骤：                                                  │
│  1. 完成角色和场景的文字描述                                 │
│  2. 确认剧本内容无误                                         │
│  3. 进入【角色工厂】生成角色图片                              │
│  4. 进入【场景工厂】生成场景图片                              │
│  5. 进入【分镜中心】设计分镜                                 │
│                                                             │
│  ⚠️ 注意：                                                  │
│  - 本阶段只需要文字描述，不需要图片                           │
│  - 图片生成在后续的资产生产阶段进行                           │
│  - 文字确认后再进行图片生成，避免资源浪费                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 九、总结

### 核心原则

1. **剧本中心只需要文字描述**
   - ✅ 角色设定：文字描述（性格、外观、年龄）
   - ✅ 场景设定：文字描述（地点、氛围、时间）
   - ❌ 不需要图片：图片在后续阶段生成

2. **工业化生产阶段划分**
   - ✅ 剧本中心（文字）→ 资产生产（图片）→ 分镜中心（镜头）→ 视频中心（视频）

3. **专业分工原则**
   - ✅ 每个阶段专注自己的专业领域
   - ✅ 文字修改成本低，图片生成成本高
   - ✅ 先在文字层面确认，再进行图片生产

---

### 关键要点

| 要点 | 说明 |
|------|------|
| **职责清晰** | 剧本中心只负责文字，资产生产负责图片 |
| **流程清晰** | 文字 → 图片 → 分镜 → 视频 |
| **成本控制** | 低成本阶段（文字）确认后再进行高成本阶段（图片） |
| **专业分工** | 编剧写文字，美术做图片，导演设计分镜 |

---

**文档位置**: `d:\trae\manju\docs\script-center-scope.md`